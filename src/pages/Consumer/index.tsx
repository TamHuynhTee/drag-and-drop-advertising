import Consumer from '@/components/PageComponent/Consumer';
import React from 'react';

type Props = {};

const ConsumerPage = (props: Props) => {
  return (
    <div>
      <Consumer />
    </div>
  );
};

export default ConsumerPage;
